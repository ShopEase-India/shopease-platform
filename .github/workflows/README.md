# GitHub Workflows

This directory contains GitHub Actions workflows used for repository automation.

Although Jenkins will be the primary Continuous Integration server for the ShopEase platform, GitHub Actions may be used for lightweight repository automation such as:

* Markdown validation
* Link checking
* Documentation linting
* Pull Request labeling
* Repository maintenance

Production application builds and deployments will be managed through Jenkins and Argo CD.

